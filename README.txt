This is a Bukkit plugin that displays the coordinates of a player.
/find prints out the coordinates, only works if player using command
is holding a compass. There is a 2 minute cooldown on the command.